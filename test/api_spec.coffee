before ->
    @url = "http://smtx.github.io/tests/xray/static.html"
describe "Valid JSON recipes", ->
    describe "Get single data", ->
        it "get page title from recipe"
        it "get numeric value formatted with regex"
        it "get all data"
        it "get some data"
        it "get transform data with regex"
        it "get ajax data"
        it "get transform ajax data wight regex"